import React from 'react';
import PropTypes from 'prop-types';
import './Settings.css';

const Settings = props => <div>User Settings</div>;

Settings.propTypes = {
  // bla: PropTypes.string,
};

Settings.defaultProps = {
  // bla: 'test',
};

export default Settings;
