import React from "react";
import PropTypes from "prop-types";
//import { Test } from './Home.styles';

const Home = props => <div className="HomeWrapper">Home</div>;

Home.propTypes = {
  // bla: PropTypes.string,
};

Home.defaultProps = {
  // bla: 'test',
};

export default Home;
